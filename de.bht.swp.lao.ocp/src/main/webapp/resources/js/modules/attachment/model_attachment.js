define([
    'underscore',
    'backbone',
    'core/models/whiteboarditem',
], function(_, Backbone,WhiteboardItem) {
    var Attachment = WhiteboardItem.extend({
        
    });
    
    return Attachment;
});
